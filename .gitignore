# Secrets — NEVER commit these
.env
*.env.local

# OpenClaw runtime data
.openclaw/

# Docker Compose overrides (auto-generated)
docker-compose.extra.yml
docker-compose.override.yml

# OS artifacts
.DS_Store
Thumbs.db
